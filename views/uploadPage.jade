extends layout.jade

block content
   h1= title
   form#fileUpload(method='post' action='/uploads/upload' enctype='multipart/form-data')
      label(for='payload') Select a file to upload:
      input#payload(type='file' name='myFiles' accept='.xlsx,.xls' multiple)
      br
      button#upload Upload
